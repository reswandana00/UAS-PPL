[project]
name = "uas"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pytest>=9.0.2",
    "selenium>=4.41.0",
    "webdriver-manager>=4.0.2",
]
